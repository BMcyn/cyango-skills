# Subtitle entity

| Type | Role | Key details |
|------|------|-------------|
| `SUBTITLE` | Caption track on the timeline | `subtitles?: IAnimation<IEntityTrack>` with `text` per keyframe. Style may use `material`/`gui` depending on renderer. |
