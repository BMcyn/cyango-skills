# Override ~/.npmrc @cyango:registry (Gitea) for this repo only — publish & install
# @cyango/* resolve to npmjs while you are in this directory. Not published (see package.json "files").
@cyango:registry=https://registry.npmjs.org/
